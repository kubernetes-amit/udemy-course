apiVersion: v1
kind: RelicationController
metadata: 
  name: nginx-ec
  labels:
    app: nginx-app
spec:
  template:
    metadata: 
    name: nginx-pod
    labels:
      app: nginx-app
      type: front-end
    spec:
      containers:
        - name: nginx-container
          image: nginx
  replicas: 3
